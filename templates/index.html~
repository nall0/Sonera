<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Posts</title>
</head>
<body>
{% for post in posts %}
<p>{{ post }}</p>
{% endfor %}

{% if posts|length == 0 %}
	(aucun post)
{% endif %}

<p>
  <form action="/post" method="POST">
    <input type="text" name="post" value="enter text here">
    <input type="submit" value="Boom !">
  </form>
</p>

</body>
</html>
